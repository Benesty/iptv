name: Test kids alt2

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test it-zone via Paris proxy
        shell: bash {0}
        run: |
          UA="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0 Safari/537.36"
          P="https://iptv-lake-three.vercel.app/api/fr?u="
          declare -A C=(
            ["Nickelodeon FR proxy"]="${P}https%3A%2F%2Ftv.it-zone.tech%2Fproxy%2F963996%3Fvoucher%3D0AYVA9ZB"
            ["Nick Junior proxy"]="${P}https%3A%2F%2Ftv.it-zone.tech%2Fproxy%2F964002%3Fvoucher%3D0AYVA9ZB"
            ["Canal J proxy"]="${P}https%3A%2F%2Ftv.it-zone.tech%2Fproxy%2F964008%3Fvoucher%3D0AYVA9ZB"
          )
          for name in "${!C[@]}"; do
            url="${C[$name]}"
            body=$(curl -A "$UA" -s -m 25 -L --max-filesize 200000 -w '\nHTTPCODE:%{http_code}' "$url" 2>/dev/null)
            code=$(printf '%s' "$body" | sed -n 's/.*HTTPCODE:\([0-9]*\)$/\1/p' | tail -1)
            if printf '%s' "$body" | grep -q '#EXTM3U'; then r="OK  "; else r="FAIL"; fi
            printf '%s | %s | %s\n' "$r" "${code:-000}" "$name"
          done | sort
